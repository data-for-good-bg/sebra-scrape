[project]
name = "sebra-etl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "gdrive-lib",
    "pandas>=2.3.3",
]

[tool.uv.sources]
gdrive-lib = { path = "../gdrive_lib", editable = true }
