[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

# TODO: add authors, description, readme etc. See https://packaging.python.org/en/latest/guides/writing-pyproject-toml
[project]
name = "sebra-scrape"
version = "0.0.1"
dependencies = [
    "requests~=2.31",
    "pandas~=2.2",
    "numpy~=1.26",
    "openpyxl~=3.1.2",
]
requires-python = ">= 3.8"